# n8n example module basis snapshot

Version: `0.1.0-scaffold.1`

This directory is the identified public n8n example-module build input for VPSmith Platform step 1. It is intentionally not a `module.yaml` implementation yet. The generic module contract and generated lifecycle are implemented only in their designated later step.

Historical n8n scripts are evidence only and are not copied here as a second source of truth.
